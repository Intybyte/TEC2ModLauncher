It is basically a copy of https://github.com/ToufouMaster/Cube-World-Mod-Launcher 's mod launcher
with 2 changed variables, it is a fairly simple program just startup the game supsended then injects the actual ModLoader
